{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "driver"
      ],
      "default": "user"
    },
    "phone": {
      "type": "string",
      "description": "Telefone de contato da motorista (WhatsApp/celular)"
    }
  },
  "required": [
    "phone"
  ]
}